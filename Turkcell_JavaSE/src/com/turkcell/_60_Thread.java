package com.turkcell;

public class _60_Thread {
	
	// Senkron : Ayný anda sadece 1 is yapabilme yetenegidir
	// Asenkron : Ayný anda 1 den fazla is yapabilma yetenegidir
	// Java ayni anda birden fazla processle islem yapabilmemizi saglar
	// Thread kullanýyorsan exception handling kullanmalýsýn (try-catch)
	
	// Multi-Thread : Hizliligi ve guvenilirligi saglar
	
	// run() = thread calistirir
	// start() = thread baslatir
	// join() = ustteki thread bitmeden devam etmez
	// wait() = threadin 2. emre kadar durmasini isteriz
	// notify() = ilgili threadin uyanmasini isteriz
	// notifyAll() = Butun threadleri ayaga kaldirir
	
}
